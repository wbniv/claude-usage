# 2026-05-21 — Session Log


## User (turn 0)

md

## User (turn 2)

- [ ] **TR‑1** (pass-26 Low): `docs/transcripts/` is tracked + special-cased in `task release`'s dirty check. Pick: gitignore it (p
urely local) OR add `task transcripts-commit` and make it part of the release checklist. [Review](docs/investigations/2026-05-20-co
de-review-pass26.md)


this is by design; not a bug

## User (turn 3)

let's review the items in the backlog i'm supposed to do

## User (turn 4)

  - SV-1: metadata.json:shell-version only lists 45–49. Ubuntu 26.04 ships GNOME 50 (~Apr 2026); the extension silently fails to
  enable. Fix is "add '50'/'51' to the list" — but I deferred it pending a smoke-test on a GNOME 50 host. Do you have access to a 
  GNOME 50 VM/machine, or do you want to add the versions speculatively?


i presume you say this because i'm not running gnome 50?
if so, you'll need to spin up a VM to test

## User (turn 5)

  Medium — lints

  - PL-3: The scraper parity lint covers regex literals but not section-anchor strings ('Plan usage limits', 'Extra usage') or DOM
  selectors that appear in both scraper.js and background.js. Scope question: which of those strings should be parity-checked? All
  of them, just the section anchors, or nothing (accept the duplication)?

implement all the permutations
compare and contrast; grade the different implementations in docs/investigations/
provide a recommendation

## User (turn 6)

do A

## User (turn 7)

todo?

## User (turn 8)

continue

## User (turn 9)

you shouldn't claim support for 50 (or any version) until it's test
only add speculative versions for the vm for testing ;-) heh

## User (turn 10)

  - AS‑1 (Medium) — _autoScrapeIfEligible seizes _fetching before the async eligibility check; ineligible fires can starve a legit   alarm for 7 min. Design call: verify revised flow doesn't introduce new SPA-nav races.

is there something i'm supposed to do on this?

## User (turn 11)

in what ways (if any) does option 1 degrade the user's experience?

## User (turn 12)

"  2. Two-phase mutex — separate _evaluating flag for eligibility, _fetching only after commit. More moving parts; easy to get
  wrong."
easy to get wrong?? really? :-O

## User (turn 13)

you often claim things are more difficult than they are, heh

## User (turn 14)

absolutely.
update plan

## User (turn 15)

what's RD-1?

## User (turn 16)

sounds good

## User (turn 17)

i need to re-install and logout/login to test IN-1, right?

## User (turn 18)

yes

## User (turn 19)

[Image #1]
you mean the refresh circular arrow icon? or something else?

## User (turn 20)

[Image #2]

## User (turn 21)

build

## User (turn 22)

will@will-Inspiron-14-7445-2-in-1:~/SRC/claude-usage$ sudo apt-get install -y --reinstall dist/claude-usage_0.11.20_all.deb
[sudo: authenticate] Password: 
Reading package lists... Done
Building dependency tree... Done
Reading state information... Done
Package dist is not available, but is referred to by another package.
This may mean that the package is missing, has been obsoleted, or
is only available from another source

E: Release 'claude-usage_0.11.20_all.deb' for 'dist' was not found

## User (turn 23)

always provide installation instructions after building a package ;-) remember that
