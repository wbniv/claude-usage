| Date | Change |
|------|--------|
| [2026-06-19](https://github.com/wbniv/claude-usage/commit/c8e5d89) | feat(macos): preferences window (NSColorWell + NSStepper) round-tripping config.json |
| [2026-06-18](https://github.com/wbniv/claude-usage/commit/4a82813) | docs(macos): record green Codemagic build + remaining live steps |
| [2026-06-18](https://github.com/wbniv/claude-usage/commit/e1e400b) | ci(macos): Codemagic workflow for the menu-bar port |
| [2026-06-18](https://github.com/wbniv/claude-usage/commit/d27506d) | feat(macos): menu-bar port — usage_core extraction, PyObjC NSStatusItem, launchd + cask |

<!--history-meta v1
c8e5d89	author	Will Norris
c8e5d89	added	1
c8e5d89	deleted	1
c8e5d89	files	1
c8e5d89	body	Native PyObjC prefs window (desktop/macos/prefs.py) — the GNOME prefs.js / KDE\nConfigGeneral.qml analog. Colour wells for the menu-bar % + popup colours and\nsteppers for warning/critical thresholds, popup bar width, and popup font size,\nall writing the SAME ~/.config/claude-usage/config.json the app polls (≤2s live\napply). Opened via a "Preferences…" (⌘,) item in the status menu; the controller\nis lazily created + retained.\n\n- usage_core: new write_ui_config() (atomic merge, 0600) + a shared _coerce()\n  validation path; load_ui_config refactored onto _coerce (one rule set for\n  reads and writes). New test_usage_core.py (7 tests): round-trip, preserve\n  other keys, drop invalid/unknown/out-of-range, 0600, str→int coercion. Suite\n  115→122.\n- prefs.py keeps only init/show + the two *_ action selectors as Obj-C methods\n  (hex↔NSColor are module functions) to avoid the BadPrototypeError trap.\n- build-app.sh bundles prefs.py (+ py2app include); ci-smoke imports it under\n  real PyObjC. Dock-ring colours + font-family picker deferred.\n\nPlan: docs/plans/2026-06-19-macos-prefs-window.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01SBMM6hGDjj6erCuffmAeqJ
4a82813	author	Will Norris
4a82813	added	4
4a82813	deleted	1
4a82813	files	1
4a82813	body	macos-test workflow is green on a real Mac (build 6a340e9a): deps, task test,\npy2app build + ad-hoc codesign, bundle verify, and the real-PyObjC ci-smoke all\npass. Plan steps 4 (build/sign) and 8 (smoke) now PASS via CI; narrow the\nremaining [verify][live] set to the interactive steps (LaunchAgent/menu-bar\nrender, server /hello, extension end-to-end, cask install, diagnostics).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01SBMM6hGDjj6erCuffmAeqJ
e1e400b	author	Will Norris
e1e400b	added	1
e1e400b	deleted	1
e1e400b	files	1
e1e400b	body	Adds codemagic.yaml — a macOS test workflow on real Mac runners, covering the\nport's [verify][live] gap that Linux + GitHub Actions can't: it runs `task test`,\n`task build-macos` (py2app, ad-hoc signed), verifies the bundle (lipo + codesign\n--verify), and runs packaging/macos/ci-smoke.py (real-PyObjC import + ported-logic\nassertions — no GUI launch). Node 24, mac_mini_m1, test-only: no signing identity,\nno secrets. GitHub Actions release.yml stays Linux-only (.deb + Chrome zip).\n\n- packaging/macos/build-app.sh: PY2APP_ARCH override so CI builds native arm64\n  (runner Pythons are single-arch); release default stays universal2.\n- packaging/macos/ci-smoke.py: reusable real-PyObjC smoke (imports usage_core +\n  the app module, asserts fmt_countdown/live_remaining/is_selectable/get_primary/\n  reset_hint against extension.js semantics).\n- plan + TODO updated: Codemagic is the macOS CI (one-time manual step — connect\n  the repo at codemagic.io with codemagic.yaml as the config source — documented\n  in the yaml header).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01SBMM6hGDjj6erCuffmAeqJ
d27506d	author	Will Norris
d27506d	added	169
d27506d	deleted	0
d27506d	files	1
d27506d	body	Adds a macOS port alongside the GNOME and KDE frontends: a menu-bar\n(NSStatusItem) app that mirrors the GNOME panel, with the local data server\nhosted in-process under a single launchd LaunchAgent, distributed via a\nHomebrew cask.\n\nSingle source of truth: server/usage_core.py extracts the pacing/colour/tier\nmath out of the hyphenated-and-unimportable generate-icon.py so the macOS app\nimports it directly — no fourth copy of the math, no new parity lint. The four\nJS<->Python twins (pacingPct/elapsedFraction/pacingSegments/colorFor) now live\nin one module; lint-scraper-parity retargets to it and still matches. generate-\nicon.py and popup-preview.py import from it (re-exported so _doc_render's pp.*\nresolves). load_config stays in generate-icon.py (test_icon monkeypatches\n_gsettings_or_none and needs same-module resolution); usage_core gains a\nGSettings-free load_ui_config() for non-GNOME frontends.\n\n- desktop/macos/: PyObjC NSStatusItem app (LSUIElement, menu-bar only),\n  statusbar_image.py, Info.plist.in. Mirrors extension.js — tier ladder,\n  critical flash, scroll-to-cycle, click-to-pin, notifications, per-segment\n  coloured pacing bars.\n- server/usage-server.py: serve_forever() entry point; cross-platform\n  _pid_alive() (/proc on Linux, os.kill elsewhere); skip the Linux dock-icon\n  spawn on darwin. test_orphan_sweep un-skipped on POSIX.\n- schema_defaults.py: CLAUDE_USAGE_SCHEMA_DIR override so the zipped-in-bundle\n  module can locate the gschema XML.\n- packaging/macos/: launchd plist, py2app build-app.sh (ad-hoc sign,\n  universal2), Homebrew cask. Taskfile build/install/uninstall-macos. usage_core\n  added to install.sh + build-deb.sh.\n- claude-usage-status.py: darwin branch (launchctl, skip gnome-extensions,\n  cross-platform Chrome prefs path).\n- MANUAL.md macOS section.\n\nVerified on Linux: task test exit 0 (115 server tests + node + all lints);\ngenerate-icon --baseline renders; ported pure logic unit-tested 14/14 against\nextension.js semantics with stubbed PyObjC. macOS bundle/launchd/cask/end-to-end\nremain [verify][live] on a Mac (plan steps 4-7, 9-12).\n\nPlan: docs/plans/2026-06-18-macos-port.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01SBMM6hGDjj6erCuffmAeqJ
-->
