# Pass-6 Fixes

**Date:** 2026-05-17
**Status:** Complete
**Triggered by:** [Pass 6 code review](../investigations/2026-05-17-code-review-pass6.md)

## Scope

Close all bug-severity items (P6-1..P6-9) and 9 of the 14 code-quality items from the pass-6 review. Bump version 0.10.5 → 0.10.6. Two commits — bugs + version, then code quality.

## Bugs (Commit A)

| ID | File | Fix |
|----|------|-----|
| BUG-P6-1 | `chrome-extension/background.js` | Always include `_anthropic_status` in POST (including `null`); server merge clears stale outage flag |
| BUG-P6-2 | `server/generate-icon.py` | Cleanup by mtime with 1 s grace window — concurrent regens no longer delete each other's icons |
| BUG-P6-3 | `server/usage-server.py` | Validate `_period_lengths` (`dict[str, int]`, values in `[0, 44640]`) |
| BUG-P6-4 | `server/tooltip.py` | Unique `.tmp.PID.NS` filename — no shared-tmp race between tooltip tick and icon regens |
| BUG-P6-5 | `server/usage-server.py` | Reject `bool` for `_timestamp` (close the P5-1-shaped gap on the timestamp field) |
| BUG-P6-6 | `server/usage-server.py` | Cap `reset_minutes` at 31 days (44640 min) |
| BUG-P6-7 | `chrome-extension/background.js` | Exact URL match on auto-scrape listener (strip `?`/`#`, compare equal) |
| BUG-P6-8 | `chrome-extension/background.js` | 5 s `AbortController` timeout on status-page fetch |
| BUG-P6-9 | `server/usage-server.py` | Origin-based CORS — only emit `Allow-Origin` for `chrome-extension://*` |
| A4 | `Taskfile.yml` | `release` preflight gates on `packaging/control` and `manifest.json` version sync |

## Code Quality (Commit B)

| ID | File | Fix |
|----|------|-----|
| CQ6-1 | `server/generate-icon.py` | Hoist `from PIL import ImageOps` to module top |
| CQ6-2 | `chrome-extension/background.js` | Single `chrome.storage.local.get(...)` call in auto-scrape listener |
| CQ6-3 | `scripts/claude-usage-status.sh` | Cache thresholds aligned to extension's 10/20 min |
| CQ6-4 | `scripts/claude-usage-status.sh` | Surface `_scrape_fail_count` + `_anthropic_status` |
| CQ5 (pass 5) | `scripts/claude-usage-status.sh` | Single Python invocation instead of three — closed by the CQ6-4 restructure |
| CQ6-8 | `gnome-extension/extension.js` | Rate-limit `Main.notify` to 1 per 5 min |
| CQ6-9 | `server/tooltip.py` | Fail-loud on unknown day in `parse_reset` (return `None` instead of defaulting to Monday) |
| CQ6-10 | `packaging/test-deb-verify.sh` | Add `py_compile tooltip.py` + `bash -n claude-usage-status` |
| CQ6-11 | `packaging/build-deb.sh` | Scope `chmod 755` to entry-point scripts (parity with `install.sh`) |
| CQ6-12 | `PRIVACY.md` | Enumerate `_anthropic_status`, `_scrape_fail_count` alongside `_period_lengths` |

## Deferred (with reasons)

- **CQ6-5** (`release` deps run before preflight): structural to Task's `deps:` semantics; needs a separate `release-preflight` task. Not worth the churn for a once-per-release annoyance.
- **CQ6-7** (`_period_lengths` accumulates labels): bounded by Anthropic's label universe. No eviction needed.
- **CQ6-13** (SPA navigation): adding `webNavigation` permission is overkill; the common paths (popup item, toolbar icon, bookmarks) hit the existing listener via hard navigations.
- **CQ6-14** (MANUAL.md example text drift): cosmetic.
- **CQ8** (pass 5 — Activities-search `Name=` overwrite): still requires a live GNOME session test.
- **A1** (no CI), **A2** (`metadata.json` version), **A3** (source+deb conflict), **A5** (`dist/` cleanup): out of scope.

## Verification

After Commit A:

1. **Validator regression** — `curl` probes for P6-3 (bad `_period_lengths`), P6-5 (`_timestamp: true`), P6-6 (huge `reset_minutes`), P6-9 (evil Origin). All expect 422 / no `Allow-Origin` header.
2. **P6-1 end-to-end** — seed cache with `_anthropic_status: {indicator: 'minor'}`, send a successful scrape with no status fetch — cache `_anthropic_status` should become `null`.

After Commit B:

3. `task test-deb-fast` — includes the new `py_compile tooltip.py` + `bash -n claude-usage-status` verification lines.
4. `claude-usage-status` — surfaces all expected sections; thresholds align with extension.

## Outcome

### Commit A — Bugs + 0.10.6 bump (213e19b)

All 9 bug IDs (P6-1..P6-9) plus A4 (version-sync preflight) landed in one commit. Files touched: `server/{usage-server.py,generate-icon.py,tooltip.py}`, `chrome-extension/{background.js,manifest.json}`, `packaging/control`, `Taskfile.yml`.

**Validator regression** (run on the working-tree `usage-server.py` after Commit A):

```
✓ P6-3 _period_lengths str:     err="'_period_lengths' must be an object"
✓ P6-3 _period_lengths negval:  err="'_period_lengths['weekly']' must be a non-negative integer ≤ 44640"
✓ P6-3 _period_lengths overcap: err="'_period_lengths['weekly']' must be a non-negative integer ≤ 44640"
✓ P6-3 _period_lengths str-val: err="'_period_lengths['weekly']' must be a non-negative integer ≤ 44640"
✓ P6-3 _period_lengths bool:    err="'_period_lengths['weekly']' must be a non-negative integer ≤ 44640"
✓ P6-3 _period_lengths valid:   err=None
✓ P6-5 _timestamp true:         err="'_timestamp' must be a number"
✓ P6-5 _timestamp valid:        err=None
✓ P6-6 reset_minutes overcap:   err='meters[0].reset_minutes must be in [0, 44640] or null'
✓ P6-6 reset_minutes ok:        err=None
```

P6-1, P6-2, P6-4, P6-7, P6-8, P6-9 verified by code inspection — small, self-contained changes with no observable behavior change on the happy path.

### Commit B — Code quality

CQ6-1, CQ6-2, CQ6-3, CQ6-4 (closes pass-5 CQ5 as a by-product), CQ6-8, CQ6-9, CQ6-10, CQ6-11, CQ6-12 all landed. Files touched: `server/{generate-icon.py,tooltip.py}`, `chrome-extension/background.js`, `gnome-extension/extension.js`, `scripts/claude-usage-status.sh`, `packaging/{build-deb.sh,test-deb-verify.sh}`, `PRIVACY.md`.

**`task test-deb-fast`** — passes (`OK`). The two new verification lines (`py_compile tooltip.py`, `bash -n claude-usage-status`) execute and pass inside the container.

**`claude-usage-status` local run** — correctly aligns with extension's 10/20 min thresholds:

```
Claude Usage — status check

  Service:    ● running (PID 450680)
  Cache:      ⚠ 11m old — extension flips to STALE at this point (plan: Max plan)
  Meter:      Current session                            73%  ███████░░░
  Meter:      All models                                 28%  ███░░░░░░░
  Meter:      Sonnet only                                15%  ██░░░░░░░░
  Meter:      Claude Design                              10%  █░░░░░░░░░
  Meter:      Daily included routine runs                 0%  ░░░░░░░░░░
  Extension:  ACTIVE (claude-usage@indri.studio)
```

The 11-min-old cache correctly trips the new stale threshold (previously it would have reported `✓ present` because the old threshold was 30 min).
