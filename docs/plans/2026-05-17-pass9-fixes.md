# Pass-9 Fixes

**Date:** 2026-05-17  
**Status:** Complete  
**Triggered by:** [Pass-9 code review](../investigations/2026-05-17-code-review-pass9.md)

## Scope

Close all actionable items from the pass-9 review across four severity tiers (critical → high → medium → low). Items resolved across multiple sessions; items closed as can't-fix or review errors are documented separately in `docs/wont-fix.md`.

## Changes

### Critical / High

| ID | File | Fix |
|----|------|-----|
| E-1 | `gnome-extension/extension.js` | Already fixed in prior session — `[Rr]` regex was already present |
| G-1 | `server/generate-icon.py` | Cap `all_pct` and `sonnet_pct` at `100.0` before passing to Cairo arc |
| S-1 | `server/usage-server.py` | Restrict CORS to the specific extension ID via `CLAUDE_USAGE_EXTENSION_ID` env var |

### Medium

| ID | File | Fix |
|----|------|-----|
| S-2 | `server/usage-server.py` | Evict `_period_lengths` keys not in current scrape's label set |
| S-3 | `server/usage-server.py` | Reject POST when `_period_lengths` exceeds 100 keys |
| E-2 | `gnome-extension/extension.js` | Replace deprecated `logError(e, msg)` with `console.error(msg, e)` |
| E-3 | `gnome-extension/extension.js` | Replace synchronous `load_contents_sync` with `load_contents_async` |
| P-1 | `gnome-extension/prefs.js` | Clamp warning/critical thresholds relative to each other in `SpinRow` handlers |

### Low (actionable)

| ID | File | Fix |
|----|------|-----|
| B-1 | `chrome-extension/background.js`, `scraper.js` | Cap weekday `parseResetMinutes` result at `60 * 24 * 31`; add test |
| B-2 | `chrome-extension/background.js` | Replace 3-second `setTimeout` delay with `MutationObserver` hydration wait |
| B-4 | `chrome-extension/background.js` | Log before silently swallowing inner error-report fetch failure |
| T-2 | `server/tooltip.py` | Preserve unrecognised `.desktop` lines instead of silently dropping them |
| T-3 | `server/tooltip.py` | Warn to stderr when no recognisable meter labels found in `format_tooltip` |
| E-4 | `gnome-extension/extension.js` | Clear stale `panel-metric` pref when stored label no longer matches a meter |
| E-5 | `gnome-extension/extension.js` | Initialise `_anyCrit` and `_flashSuppressed` in `_init` (not first use) |
| E-6 | `gnome-extension/extension.js` | Persist notification cooldown timestamp to `~/.cache/claude-usage/notif-ts` across reloads |
| G-2 | `server/generate-icon.py` | Document substring match caveat on `find_meter` lambda |
| G-3 | `server/generate-icon.py` | Log warning when color fallback fires in `load_config` |

### Scraper unit tests (added alongside B-2)

45 tests across 6 suites (`isHydrated`, `parseResetMinutes`, `doScrape` sections 1–3) in `chrome-extension/test/scraper.test.js`. Run with `task test-scraper`.

## Closed without code change

| ID | Reason |
|----|--------|
| B-3 | Can't fix — tab ID doesn't exist until `tabs.create` returns; gap is a single microtask turn, unreachable by Chrome's SW scheduler |
| E-6 | Fixed (was previously marked acceptable) |
| M-1 | Review error — README has no version string |
| I-1 | Review error — `install.sh` line 2 is already `set -euo pipefail` |

See `docs/wont-fix.md` for permanent rationale.

## Verification

```
task test-scraper   # 45/45 pass
node --check chrome-extension/background.js
node --check chrome-extension/scraper.js
```
