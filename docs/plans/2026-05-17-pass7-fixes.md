# Pass-7 Fixes

**Date:** 2026-05-17
**Status:** Complete
**Triggered by:** [Pass 7 code review](../investigations/2026-05-17-code-review-pass7.md)

## Scope

Close all 4 items from the pass-7 review — 2 low-severity bugs + 2 code-quality items. Total ~15 LOC across 2 files. Single commit; no version bump (sub-release-threshold work).

## Changes

| ID | File | Fix |
|----|------|-----|
| BUG-P7-1 | `chrome-extension/background.js` | Clear `chrome.storage.local.claude_usage` on any 4xx — not just 2xx — so a buffer rejected by a stricter validator stops retrying forever |
| BUG-P7-2 | `server/usage-server.py` | Validate `meters[*].count` and `meters[*].total` as non-negative ints ≤ 10⁹ — closes the layout-distortion vector flagged in pass 7 |
| CQ-P7-1 | `server/usage-server.py` | Drop `stderr=subprocess.DEVNULL` on the `generate-icon.py` spawn so failures surface in `journalctl --user-unit=claude-usage-fetch` |
| CQ-P7-2 | `server/usage-server.py` | Run-once startup sweep for orphan `~/.local/share/applications/claude-usage.desktop.tmp.*` files (left over from crashed writes under the BUG-P6-4 unique-tmp scheme) |

## Verification

1. `task test-deb-fast` — full smoke test (~10 s)
2. Extend the BUG-P6-3/5/6 validator probes with a `count`/`total` case
3. Manual: confirm `generate-icon.py` errors appear in journal if I temporarily break it

## Outcome

All 4 items landed in one commit. Files touched: `chrome-extension/background.js`, `server/usage-server.py`.

**Validator regression** (run against working-tree `usage-server.py`):

```
✓ P7-2 count overcap:  err='meters[0].count must be a non-negative integer ≤ 10^9 or null'
✓ P7-2 total overcap:  err='meters[0].total must be a non-negative integer ≤ 10^9 or null'
✓ P7-2 count negative: err='meters[0].count must be a non-negative integer ≤ 10^9 or null'
✓ P7-2 count bool:     err='meters[0].count must be a non-negative integer ≤ 10^9 or null'
✓ P7-2 count string:   err='meters[0].count must be a non-negative integer ≤ 10^9 or null'
✓ P7-2 count/total ok: err=None
✓ P7-2 count missing:  err=None
✓ P6-3 still works:    err="'_period_lengths['weekly']' must be a non-negative integer ≤ 44640"
✓ P6-5 still works:    err="'_timestamp' must be a number"
✓ P6-6 still works:    err='meters[0].reset_minutes must be in [0, 44640] or null'
```

**Orphan sweep**: created 3 fake `.desktop.tmp.PID.NS` files; `_sweep_orphan_tmps()` removed all three.

**`task test-deb-fast`**: OK (no test-deb-verify.sh changes this round; existing checks all pass).

BUG-P7-1 and CQ-P7-1 verified by code inspection — single-line changes with clear semantics.
