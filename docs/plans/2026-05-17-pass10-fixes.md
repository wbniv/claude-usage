# Pass-10 Fixes

**Date:** 2026-05-17  
**Status:** In progress (high + medium complete; low items open)  
**Triggered by:** [Pass-10 code review](../investigations/2026-05-17-code-review-pass10.md)

## Scope

8 findings from pass-10. High and medium fixed in one commit; low items tracked below.

## Changes (complete)

| ID | File | Fix |
|----|------|-----|
| E-7 | `gnome-extension/extension.js` | Add `NOTIF_CRIT_TS_FILE` constant; read `_lastCritNotifyTs` from it in `_init`; write it after each critical pacing notification. E-6 only persisted the tier cooldown. |
| E-8 | `gnome-extension/extension.js` | Validate `popup-font-family` GSettings value against `/^[\w\s,'"-]+$/` before interpolating into CSS rule; fall back to `'monospace'` on mismatch. |
| K-1 | `packaging/build-chrome-zip.sh` | Add `-x "test/*"` to zip command to exclude test files from CWS upload. |

## Remaining (low priority)

| ID | File | Description |
|----|------|-------------|
| B-5 | `background.js`, `scraper.js` | `parseInt` missing radix 10 |
| M-2 | `manifest.json` | `host_permissions` broader than needed |
| K-2 | `postinst`, `postrm` | `set -e` only — missing `-uo pipefail` |
| E-9 | `extension.js` | `_watchFile` failure not retried |
| S-4 | `usage-server.py` | Orphan sweep races with live `.tmp.*` writer |

## Verification

```
node --check chrome-extension/background.js
node --check chrome-extension/scraper.js
task test-scraper   # 45/45 expected
```
