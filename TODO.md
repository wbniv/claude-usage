# TODO

> **Next session — read [docs/HANDOFF.md](docs/HANDOFF.md) first.** Firefox support
> shipped + live-validated; the `FF-1` fix (replace DOM-scrape with the usage JSON
> API) is mid-flight — Phase 1 (mapping) done, the rest is gated on a live de-risk
> that needs a browser logged into claude.ai.

- [ ] self-update

## Fixes

- [ ] **2026-06-19** [verify] [live] macOS preferences window — PyObjC `PrefsController` (`desktop/macos/prefs.py`): `NSColorWell` colour wells (panel `%` + popup colours) + `NSStepper` thresholds/bar-width/popup-font-size, round-tripping the same `~/.config/claude-usage/config.json` the app polls (≤2s live apply). "Preferences…" (⌘,) item in the status menu. New `usage_core.write_ui_config()` + shared `_coerce()` (single validation path for read+write) with `test_usage_core.py`. Dock-ring colours + font-family picker deferred. CI covers the config layer + real-PyObjC import; **[live]:** open prefs on a Mac, change a colour/threshold, confirm the menu bar recolours + persists. [Plan](docs/plans/2026-06-19-macos-prefs-window.md).

- [ ] **2026-06-18** [verify] [live] macOS port — **landed** (Linux side tested, `task test` green 115 server + lints): `server/usage_core.py` extracts the pacing/colour/tier math out of `generate-icon.py` (single source of truth — no fourth copy, `lint-scraper-parity` retargeted) + `load_ui_config()`; macOS menu-bar app `desktop/macos/` (Python + PyObjC `NSStatusItem`, in-process server thread, menu-bar-only `LSUIElement`); `usage-server.py` `serve_forever()` + `_pid_alive`/darwin icon-spawn guards; launchd LaunchAgent + py2app `build-app.sh` + Homebrew cask under `packaging/macos/`; `claude-usage-status` darwin branch; MANUAL macOS section; `usage_core.py` added to `install.sh` + `build-deb.sh`. macOS CI **green on Codemagic** (`codemagic.yaml`, `mac_mini_m1`, build `6a340e9a`): `task test` + `task build-macos` (py2app + ad-hoc codesign) + bundle verify + `ci-smoke.py` (real PyObjC) all pass on a real Mac. Three real-PyObjC bugs fixed en route (AppKit vs Foundation attribute keys, `@objc.python_method` selector-arity, CI `lipo` glob). Drivable via the [`codemagic-build`](~/.claude/skills/codemagic-build/) skill (`task cm-build`) which pulls the log artifact. **Remaining `[verify][live]` (interactive Mac, not headless CI):** LaunchAgent load + menu-bar render, in-process server `/hello`, extension end-to-end, cask install/uninstall, diagnostics (plan steps 5–7, 11–12). Dock-rings deferred. [Plan](docs/plans/2026-06-18-macos-port.md).

- [ ] **2026-06-02** [verify] 100%-state behaviour: at 100% the panel label shows `⏱<countdown-to-reset>` (live, minute-ticking) instead of `100%` for any maxed meter (soonest-resetting wins); when the countdown hits 0 the Chrome/Firefox extension force-refreshes very aggressively (1-min `fetch-on-reset` alarm across the boundary, +1-min lead, stand-down on confirmed reset, `RETRY_CAP` backstop) so the panel reflects the new window without waiting 7 min. GNOME + KDE display, `background.js` scheduling, MANUAL/README docs. [Plan](docs/plans/2026-06-02-100pct-countdown-and-reset-refresh.md). Commit `0ba9c6d`.

- [ ] **2026-06-01** [verify] **[live]** **KQ-1** (pass-29): KDE scroll-to-cycle was relocated from a dead root-`PlasmoidItem` `MouseArea` to a `WheelHandler` in `CompactRepresentation.qml`. `lint-qml` (qmllint 6.10.2) + `lint-kde-parity` pass, but wheel-over-panel-icon behaviour needs confirming on a real Plasma 6 session (`task test-kde-live`). [Review](docs/investigations/2026-06-01-code-review-pass29.md).

- [ ] **2026-05-30** [verify] **[live]** Remaining: GNOME‑45 notify fallback (needs a 45 box) + run `test-gnome` in CI (gaps 3–4 in [plan](docs/plans/2026-05-30-code-review-fixes.md)). **KDE DONE** — live-verified on Plasma 6: `file://` XHR hangs at readyState 1 in the plasmoid sandbox, so `loadData()` switched to the executable engine (`cat`); Qt6 read-only `implicitWidth` fixed; screenshot `docs/plans/screenshots/kde-plasmoid-live.png`.

- [ ] **2026-05-22** Add pre-flight dep checks in `install.sh` (`glib-compile-schemas` hard-fail, `systemctl --user` hard-fail, `gnome-shell` 45–50 warn) + relabel MANUAL.md Option B "From source" → "From a clone" + add Option C `curl | bash` one-liner — [plan](docs/plans/2026-05-22-curlbash-installer.md)

## Deferred
- [ ] **2026-06-01** **KQ-2** (pass-29, KDE): count/total ("Daily included routine runs") and Extra-usage meters render as `N%` + a bar in the plasmoid; GNOME shows `count/total` (no bar) and spent/balance. Branch `MeterRow.qml` on `meter.count`/`meter.spent`. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-06-01** **KQ-3** (pass-29, KDE): the plasmoid has no eligibility / Sonnet-0% filter — it shows a dead "Sonnet 0%" popup row, offers it as a panel-metric radio, and scroll-cycles onto it. GNOME filters these (`_isSelectable`/`visibleMeters`); mirror the predicate in `FullRepresentation.qml` + `cycleMeter`. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-06-01** **LK-1** (pass-29, lints): extend `lint-kde-parity` config-parity to compare gschema `<range>` vs `ConfigGeneral.qml` SpinBox `from`/`to` (document the threshold warn/crit offset exception). Today only `<default>` is guarded, so UI bounds drift silently (bar-width to:40 vs gschema 20, panel-icon to:64 vs 32). Also: `lint-security-doc` covers SECURITY.md only — consider extending to PRIVACY.md's permission table (see DOC-1). [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-06-01** **KQ-7** (pass-29, KDE/server): latent ARGB-vs-RGBA mismatch — Qt `color.toString()` emits `#AARRGGBB`, `generate-icon.py:hex_to_rgba` parses 8-digit as `#RRGGBBAA`. Inert today (KDE ColorButton is opaque → 6-digit); add a guard/comment if alpha is ever enabled. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-06-01** **KQ-8** (pass-29, KDE): `fontCombo`'s `find(editText)` at `Component.onCompleted` can highlight the wrong dropdown entry if `cfg_popupFontFamily` loads after the child completes (persisted value unaffected). Bind `currentIndex` reactively or defer via `Qt.callLater`. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-06-01** **BUF-1** (pass-29, server): `usage-server.py` keeps `_buffered_at` in `usage.json` after a later live scrape (asymmetric with the `_ext_version_mismatch` drop). Inert — no consumer reads it from the cache — so cache-hygiene only; drop it on a non-buffered POST if the merge is touched. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [ ] **2026-05-31** Full qmllint type-check for the plasmoid: `lint-qml` currently disables the module-resolution categories (Plasma QML modules absent in CI), so it's a syntax/structural gate only. A Foundry-desktop image (Ubuntu 26.04 + Plasma 6 + org.kde.plasma.*/Kirigami QML modules — see `~/SRC/foundrylinux.org`) would let qmllint resolve `PlasmoidItem`/`Plasma5Support`/`cfg_*` aliases and type-check for real. Dogfood Foundry.
- [ ] **2026-05-31** Server reads the GNOME gschema (`org.indri.claude-usage`) for icon-render defaults even on KDE-only boxes (`server/generate-icon.py`, `server/schema_defaults.py`). The `.deb` ships the gschema regardless so it resolves, but KDE's canonical config is KConfig + `~/.config/claude-usage/config.json` — the dock icon should prefer that on Plasma. Architectural quirk surfaced during the cross-desktop rename; not a naming issue. [Plan](docs/plans/2026-05-31-cross-desktop-rename.md).
- [ ] **2026-06-02** **FF-1** (Firefox): the FF port's non-persistent event-page background (`background.scripts`) doesn't reliably wake for the 7-min `alarms` scrape — live test on Firefox 151 left the cache ~10 min stale vs the live page; a forced toolbar scrape refreshes but lags (the event page unloads during the up-to-30 s hydration-wait). **`background.service_worker` is NOT the fix** (tried 2026-06-02): Firefox keeps it behind the `extensions.backgroundServiceWorker.enabled` pref — off by default through 151 — so a SW build fails to install ("background.service_worker is currently disabled. Add background.scripts."). Event page is the only background type that installs on stock Firefox. Real fix direction (better than a keepalive): replace the open-a-tab DOM scrape with a direct fetch of `GET /api/organizations/{org}/usage` — the JSON the SPA itself fetches (~565 B, ~400 ms; found via the Network tab). A tabless fetch finishes inside the event-page wake window, closing FF-1, and removes the fragile text-scraping on both browsers. Groundwork committed (`ce93dc3`): tested `chrome-extension/usage-api.js` mapping (API → the existing `meters[]` shape) + plan [docs/plans/2026-06-02-usage-api-fetch.md](docs/plans/2026-06-02-usage-api-fetch.md). **Remaining (needs a logged-in claude.ai browser):** live de-risk the cross-origin fetch (Approach A) + settle org-id discovery, then wire into `background.js` (API primary, DOM-scrape fallback). The chrome→browser shim (`chrome-extension/chrome-compat.js`) is unaffected. [Plan](docs/plans/2026-06-02-firefox-wrap-up-push-trim-screenshots-log-follow-u.md).

## Done
- [x] 2026-06-01 — Pass-30 (verification): all 15 pass-29 fixes verified correct & regression-free by an adversarial agent + direct read; 0 findings, nothing new in the changed surface. Review loop terminates cleanly (precedent: passes 22/25/28). [Review](docs/investigations/2026-06-01-code-review-pass30.md).
- [x] 2026-06-01 — Pass-29 landing: 15 of 21 findings closed; 6 deferred (KQ-2/3/7/8 KDE design, LK-1 lint range-coverage, BUF-1 cache hygiene) + KQ-1 [verify][live]. Headline: SRV-1 (uncatchable `Gio.Settings.new` SIGTRAP on a missing gschema → dock icon dead on KDE/dev installs) + KQ-1 (scroll-to-cycle wired to a dead root-`PlasmoidItem` MouseArea). Server suite 105→110. [Review](docs/investigations/2026-06-01-code-review-pass29.md).
- [x] 2026-05-31 — Cross-desktop rename: GNOME schema `org.gnome.shell.extensions.claude-usage` → `org.indri.claude-usage` (matches KDE plugin id); backends moved to `desktop/{gnome,kde}/` + launcher to `desktop/launcher/`; docs de-GNOME-ified (KDE install/config/troubleshooting); fixed MANUAL reset-countdown threshold 24 h → 12 h to match code. No migration (no released users). `task test` green. [Plan](docs/plans/2026-05-31-cross-desktop-rename.md).
- [x] 2026-05-30 — Code-review fixes landed (`8818294` + BASE‑2/3/4/5/6 + KDE‑2 follow-ups): KDE plasmoid KDE‑1..5 (shipped non-functional), DIFF‑1..4, BASE‑1..6, KDE‑2 pacing/URL parity; new `lint-kde-parity` (5 checks, each negative-tested) + SW runtime tests; scraper 57/57, server 104/104. Only [verify] [live] KDE/GNOME‑45 + BASE‑5c (design choice) remain. [Plan](docs/plans/2026-05-30-code-review-fixes.md).
- [x] 2026-05-21 — IN‑1 closed: removed `"tabs"` from manifest permissions — `host_permissions` sufficient for `tabs.query({url})`. Tested: "sent 5 meters to local server" confirmed, no errors after reload.
- [x] 2026-05-21 — RD‑1 closed: `chrome.idle.onStateChanged` 'active' handler now debounces against `_last_scrape_ts` with `WAKE_MIN_INTERVAL_MS = INTERVAL_MINUTES * 60 * 1000` (7 min); lock/unlock cycles no longer burn a claude.ai page-load each, CI-2's wake-from-suspend purpose preserved (storage error → falls through to fire). [Plan](docs/plans/2026-05-21-rd1-idle-debounce.md).
- [x] 2026-05-21 — SV‑1 closed: GNOME 50 supported (tested via Docker smoke test — 9/9 checks pass); `task test-gnome` + weekly GH Actions version-check workflow added. [Plan](docs/plans/2026-05-21-sv1-gnome50-support.md).
- [x] 2026-05-21 — AS‑1 closed: `_autoScrapeIfEligible` split into two-phase mutex (`_evaluating` for eligibility, `_fetching` for scrape) — alarm-fired fetchUsage no longer starved by ineligible auto-scrape fires; R-1 invariant preserved. [Plan](docs/plans/2026-05-21-as1-two-phase-mutex.md).
- [x] 2026-05-21 — PL‑3 closed: `lint-scraper-parity` gained `check_anchor_strings()` — allowlist pins `'Plan usage limits'`/`'Extra usage'` in both files + the Extra-toggle DOM selector in background.js. [Plan](docs/plans/2026-05-21-pl3-anchor-strings-lint.md), [Investigation](docs/investigations/2026-05-21-pl3-scraper-string-parity.md).
- [x] 2026-05-21 — TR‑1 dismissed: `docs/transcripts/` tracking + release dirty-check exemption is by design (purely local session artifacts, not a bug).
- [x] 2026-05-20 — Pass-27 landing: 1 of 1 closed (RT-1: NM-1 missing regression test for meters:null rejection). Suite 148→149. [Review](docs/investigations/2026-05-20-code-review-pass27.md).
- [x] 2026-05-20 — Pass-26 landing: 23 of 29 closed in 5 commits; 6 deferred (AS-1 mutex, RD-1 debounce, SV-1 GNOME 50, PL-3 scraper strings, TR-1 transcripts, IN-1 tabs perm). Headline: UX-1 (popup flicker), PVS-1 (over-pace rounding), RL-1 (release gate), PL-1 (parity lint string coverage), NM-1 (meters:null poison), GI-1 (hardcoded #e03030). Suite 147→148. [Review](docs/investigations/2026-05-20-code-review-pass26.md).
- [x] 2026-05-20 — Pass-24 landing: 1 Low closed (EF-2: third `elapsed_fraction` twin in popup-preview.py was missed by EF-1's pass-23 sweep — null-meter guard added). [Review](docs/investigations/2026-05-20-code-review-pass24.md).
- [x] 2026-05-20 — Pass-23 landing: closed 8 of 8 Lows + 1 Info in 3 commits (`112d1f3`, `9d7c1eb`, `<next>`); 0 deferred. Headline: CAP-1 (sibling `cap()` had UT-1's UTF-16 surrogate-pair bug) + PS-1 (parity lint extended to cover pacingSegments/colorFor — 4 pairs now). [Review](docs/investigations/2026-05-20-code-review-pass23.md).
- [x] 2026-05-20 — Pacing visualization landed: `extension.js` and `generate-icon.py` now render the tick (`┊` in the popup bar, radial grey marker on the dock ring) when under-pace, and a two-tone split (on-pace green/blue + over-pace tier color) when over-pace. Floor (max(15, period*0.05)) suppresses both. `lint-pacing-parity` extended to also check `elapsedFraction ↔ elapsed_fraction`. Docs PNGs regenerated. [Plan](docs/plans/2026-05-20-pacing-viz-tick-overpace.md).
- [x] 2026-05-20 — JS-1: `gnome-extension/_defaults.js` is now generated from the gschema XML (`scripts/gen-js-defaults.py`); `extension.js`'s `safeColor` reads from it instead of hardcoded hex literals. New `task lint-js-defaults` asserts the artifact stays in sync; `install.sh` + `build-deb.sh` regenerate on every install/build. [Review](docs/investigations/2026-05-20-code-review-pass18.md).
- [x] 2026-05-20 — UT-1: Statuspage `trunc` is now code-point-aware (`[...s].slice(0, 117)`); UTF-16 surrogate-pair splits can no longer produce invalid output. [Review](docs/investigations/2026-05-20-code-review-pass18.md).
- [x] 2026-05-20 — TT-1: `format_tooltip` "no recognisable meter labels" warning is now one-shot per process (was firing 1440×/day from `_tooltip_tick`). [Review](docs/investigations/2026-05-20-code-review-pass18.md).
- [x] 2026-05-20 — Pass-21 landing: closed 3 of 3 Lows in 1 commit; 0 deferred. BL-1 (load-test handler removed too early; intended diagnostic was dead code), DC-1 (AR-2 comment block stitched into postUpdate's doc), DC-2 (pass-20 doc arithmetic off-by-one). Slash command also gained the carry-forward-TODOs convention (commit `30b2f8e`). Suite still 146. [Review](docs/investigations/2026-05-20-code-review-pass21.md).
- [x] 2026-05-20 — Pass-20 landing: closed 5 of 5 findings in 2 commits (`8ca7b9a`, `e2a6256`); 0 deferred. Headline: AR-2 Critical (pass-19's AR-1 added `if (_fetching) return;` to restoreActionStatus, but `_fetching` was declared AFTER the top-level call — Temporal Dead Zone ReferenceError on every SW load, AT-2 broken). Added a vm-sandboxed load-time smoke test (`background-load.test.js`) to catch the class. Slash command also gained the live strikethrough step (commit `4cb1a6d`). Suite: 95 server + 51 scraper = 146 (was 94 + 50 = 144). [Review](docs/investigations/2026-05-20-code-review-pass20.md).
- [x] 2026-05-20 — Pass-19 landing: closed 20 of 20 substantive findings in 3 commits (`862a5a3`, `432078f`, `86a2f56`); 0 deferred. Headline: SL-1+SL-2 (the SD-1 lint I added in pass-18 was bypassable two ways — substring match + missing-key silent pass) + LP-1 (pass-18's L2-2 wired a consumer to a signal the producer never sent — the entire feature was dead until fixed). Slash command also gained the loop step (commit `7102d20`). Scraper suite: 45 → 50; server suite still 94. [Review](docs/investigations/2026-05-20-code-review-pass19.md).
- [x] 2026-05-20 — Pass-18 landing: closed 23 of 26 findings in 6 commits (`a1c012e`..`cd81ca5`); 3 deferred (JS-1, TT-1, UT-1 — design calls). Headline: TS-2 critical (orphan sweep PID-guard bypassed by wrong field index, silently deleting live in-flight tmps — a regression I introduced in pass-17 MS-1). New CI lint: `lint-security-doc.py`. Suite: 87 → 94. [Review](docs/investigations/2026-05-20-code-review-pass18.md).
- [x] 2026-05-20 — Pass-17 full landing: all 39 findings closed in 10 commits (`6533877`..`282a996`). Architectural change: `server/schema_defaults.py` is now the SOT for every gschema-tied constant; 8 drift findings collapsed at once. Test suite: 57 → 87. [Review](docs/investigations/2026-05-20-code-review-pass17.md).
- [x] 2026-05-20 — Dock icon size fix: `generate-icon.py` now emits at 48/64/96/128/256 hicolor sizes (was 128 only) so XDG lookup stops falling through to the .deb's ringless 64x64 baseline; auto-rebuilds the user-local icon-theme cache when a new size dir appears. Commits: `da6a2ac` (multi-size), `0dc6243` (cache refresh).
- [x] 2026-05-20 — Pacing parity lint: extended `scripts/lint-scraper-parity.py` to compare numeric constants in `pacingPct` (extension.js) vs `pacing_pct` (generate-icon.py); catches constant drift on `15`, `0.05`, etc.
- [x] 2026-05-20 — Pacing-aware threshold range → 0.11.19 (`<range max>` 99 → 500 on threshold-warning + threshold-critical; pacing is uncapped, schema cap was stale from pre-pacing semantics). [Plan](docs/plans/2026-05-20-pacing-threshold-range-0-11-19.md).
- [x] 2026-05-19 — Pass-16 L+I findings + TF‑1 → 0.11.18 (R‑1 autoscrape race, WP‑1 period-scaled pacing floor, D‑1/TF‑1 stable Icon=claude-usage via icon-theme dir, D‑2 startup icon refresh, N‑1 toast reword, I‑1 nuke-and-recreate install dirs; 3 new pacing tests). [Plan](docs/plans/2026-05-19-fix-pass-16-medium-bugs-0-11-17.md). Commit: `095c3e1`.
- [x] 2026-05-19 — Pass-16 M findings → 0.11.17 (TH‑1 stale-threshold drift, V‑2 top-level filter, PL‑1 eviction guard, V‑3 empty-string labels; 6 new tests). [Plan](docs/plans/2026-05-19-fix-pass-16-medium-bugs-0-11-17.md). Commit: `35bbb91`.
- [x] 2026-05-19 — Pacing early-period false-critical → 0.11.16 (15-min time-based floor in pacingPct + pacing_pct; 13-case pytest regression). [Plan](docs/plans/2026-05-19-pacing-early-period-fix-0-11-14.md). Commits: `1eacc51` (fix), tag `v0.11.14` (failed CI: missing cairo), `c9f7075` + tag `v0.11.15` (failed CI: BASE_ICON discovery), `<next>` (XDG stub + bump 0.11.16).
- [x] 2026-05-19 — Pass-15 L+I findings → 0.11.13 (HM‑2, SC‑2, I‑2, SC‑3, PR‑1, L‑3, CI‑4). [Plan](docs/plans/2026-05-19-fix-pass-15-low-info-findings-0-11-13.md). Commit: `ea28b71`.
- [x] 2026-05-19 — Pass-15 H+M findings → 0.11.12 (U‑1 deploy gap, TS‑1 timestamp bound, T‑8 parser parity). [Plan](docs/plans/2026-05-19-fix-pass-15-high-medium-findings-0-11-12.md). Commits: `ef3a852`, `574c32c`.
